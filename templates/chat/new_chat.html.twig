{% extends 'base.html.twig' %}

{% block body %}
    {{ form_errors(form) }}
    {{ form_start(form) }}
    <button type="submit" class="btn">Создать новый чат</button>
    {{ form_end(form) }}
{% endblock %}
